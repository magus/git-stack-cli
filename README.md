# git-stack-cli

> create stacked diffs (PRs) on Github from a single branch by grouping commits

## install

```bash
npm i -g git-stack-cli

git stack
```

## development

```bash
npm run dev
npm link

git stack --verbose
```
